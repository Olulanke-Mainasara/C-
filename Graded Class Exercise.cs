using system;

namespace Graded_Class_Exercise
{
    class Qnumbers
    {
        public void get(int a, int b, int c)
        {
            private int one = a;
            private int two = b;
            private int three = c;
        }
        public get_a()
        {
            console.writeline("Enter the value for a: ");
            a = convert.Toint32(console.readline());
        }
        public get_b()
        {
            console.writeline("Ente the value for b: ");
            b = convert.Toint32(console.readline());
        }
        public get_c()
        {
            console.writeline("Enter the value for c: ");
            c = convert.Toint32(console.readline();)
        }
        public getDiscriminant()
        {
            dicr = math.pow(b, 2) - 4 * a * c;
        }
        public getRoot1()
        {
            r1 = (-b) + math.sqrt(dicr)/2 * a;
        }
        public getRoot2()
        {
            r2 = (-b) - math.sqrt(dicr)/2 * a;
        }
    }   
}